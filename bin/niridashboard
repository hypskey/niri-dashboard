#!/bin/sh
ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
if [ -x "$ROOT/.venv/bin/python" ]; then
    exec "$ROOT/.venv/bin/python" "$ROOT/src/niridashboard/main.py" "$@"
fi
exec python3 "$ROOT/src/niridashboard/main.py" "$@"
